# .battleclaw/

User context files placed here are auto-loaded into every agent session.

Examples:
  - project.md    — project description, coding standards
  - context.md    — current sprint goals, architecture notes
  - personas.md   — custom agent persona overrides
